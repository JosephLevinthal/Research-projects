from numpy import *
from numpy.linalg import *

vet = array(eval(input("insira:")))

total = zeros(shape(vet,dtype)
for i in range(shape(vet)[0]):
	  total[i] = min(vet[i,:])

for j in range(size(total)):
	a = sum(total)
	b = shape(vet)[0]
print(round((a/b),2))



