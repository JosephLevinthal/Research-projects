# Teste todas as possibilidades de entradas 'H', 'h' e 'x'
# Se seu programa funciona para apenas um caso de tese, isso não quer dizer que ele vai funcionar para todos os outros
# Teste seu código aos poucos.
# Não teste tudo no final, pois fica mais difícil de identificar erros.
# Use as mensagens de erro para corrigir seu código.
from math import *
H = float(input("Digite aqui a altura total do tanque: "))
h = float(input("Digite aqui o nivel de combustivel no tanque: "))
r = float(input("Digite aqui o raio dos bojos semiesfericos inferior e superior: "))
V = pi * r ** 2 * + (2 * ((pi / (x ** 2)) * (3 * r - x)) / 2) 
if ((h > 0) and (H > 0) and (r > 0)):
	print("Entradas:", H, h, r)
	print(V, "litros")