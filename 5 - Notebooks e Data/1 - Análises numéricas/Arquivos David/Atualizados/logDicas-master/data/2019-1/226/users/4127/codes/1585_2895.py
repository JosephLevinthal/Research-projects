quantidade=float(input("total de jogos a serem comprados: "))
valor=float(input("valor unitario de cada jogo: "))
frete=float(input("valor do frete: "))

total=((quantidade*valor) + frete)
print(total)