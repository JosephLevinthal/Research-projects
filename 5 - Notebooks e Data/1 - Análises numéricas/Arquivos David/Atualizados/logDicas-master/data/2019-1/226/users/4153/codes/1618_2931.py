from math import *
h = float(input("Insira a altura do objeto: "))     # h = altura
g = 9.81                                            # g = aceleracao da gravidade

v = (2*g*h)**0.5                                    # v = formula

print(v)