from numpy import * 
#z = zeros(size(x), dtype=int)
x = array(eval(input("quantidade: ")))
z = zeros(size(x), dtype=int)

#v = 0
cont = 0
for i in x:
	if i %5 == 0:
		cont = cont + 1
print(cont)
for i in range(x):
	
	

	
		