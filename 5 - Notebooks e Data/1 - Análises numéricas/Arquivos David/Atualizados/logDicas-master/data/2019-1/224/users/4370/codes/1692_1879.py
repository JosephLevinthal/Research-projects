# Ao testar sua solução, não se limite ao caso de exemplo.
extras = float(input())
horas_falta = float(input())

h = extras - 1/4 * hora_falta

faltas = horas_faltas % 24
print(extras , "extras e" ,faltas)
if(h < 400):
	hfkhf,,h