from numpy import*

#quantidade de aluno matriculado por sala
vet = array(eval(input("quantidade de aluno:")))

a = 0
z = zeros(, dtype=int)
var = z + 5
for i in size(vet):
	if (vet % var) == z: 
	 	a = a + 1
	 	print (a)
