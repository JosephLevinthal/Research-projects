from numpy import*
from numpy.linalg import*
n = array(eval(input("x, y, z")))
mat = array([[8, 3, 1],[5, 12, 10], [1, 3, 2]])
contador = shape(mat)[1]
day = n.T
fim = dot(mat,day)
for i in range (mat):
	ac = zeros(fim[:,j])
