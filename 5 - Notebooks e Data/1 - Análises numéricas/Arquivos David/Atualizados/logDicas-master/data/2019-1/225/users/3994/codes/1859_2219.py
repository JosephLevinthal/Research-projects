from numpy import*
N = array(eval(input("Digite a matriz: ")))
g=0
for i in range(shape(N)[0]):
	for j in range(shape(N)[0]):
		if(i>j):
		
		g = g+1

		
g=0
for j in range(shape(N)[1]):
	for i in range(shape(N)[1]):
		if()
		
		
		
		
		
		
	
	