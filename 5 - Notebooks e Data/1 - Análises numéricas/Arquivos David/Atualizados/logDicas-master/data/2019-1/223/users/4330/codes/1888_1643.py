from numpy import*
from numpy.linalg import*

aluno=array(eval(input("Notas dos alunos: ")))
qap=0 #quantidade de alunos aprovados
aluno=aluno.T
for i in range(shape(aluno)):
	if aluno[0] >= 5:
		qap = qap + 1
	else:
		qap = qap
	for i in range(shape(aluos)[i:j]):
		for i in zeros(i,dtype=int)
		
	
