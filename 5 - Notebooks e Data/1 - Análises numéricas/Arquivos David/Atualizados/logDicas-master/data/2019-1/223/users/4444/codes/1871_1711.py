from numpy import *
from numpy.linalg import *
notas=array(eval(input("Digite a matriz de notas:  ")))
numero=int(input("Digite um nummero: "))
soma=sum(notas)[0]
if(soma>=15):
	total=total+1
	print('APROVOU')
if(soma1>=15):
	total1=total1+1
	print('APROVOU')
if(soma2>=15):
	total2=total2+1
	print('PASSOU')
somatotal=total+total1+total2
if soma<15:
	print("REPROVOU")
