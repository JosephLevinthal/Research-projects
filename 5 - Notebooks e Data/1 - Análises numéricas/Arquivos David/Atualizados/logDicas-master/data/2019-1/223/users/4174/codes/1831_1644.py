from numpy import*

rep = array(eval(input("quantidade de alunos reprovados:")))

cont = (0)

for i in range(size(rep)):
	if (rep[i] < 5.0 ):
		rep[i] = rep[i] + 1
		
ind = zeros(rep, dtype = int):
a = 0 
for i in range(size(rep)):
	if(i < size(rep))
		
		

