from numpy import*
from numpy.linalg import*

notas = array(eval(input("Digite a matriz: ")))
alunos = int(input("Digite o numero do aluno: "))

for i in shape(notas):
	if

soma = sum(notas)/size(notas)
	
if(soma >= 5):
	print("PASSOU")
else:
	print("REPROVOU")
		