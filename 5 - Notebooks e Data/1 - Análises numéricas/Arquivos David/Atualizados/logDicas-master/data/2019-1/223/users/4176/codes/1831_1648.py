from numpy import*

freq = array(eval(input("Digite: ")))
rep = 0
vet = zeros(size(freq), dtype=int)


for elemento in freq <= 70:
	rep += 1
	print(rep)
	
for elemento in freq:
	vet[i] = freq[i]
	print(vet)
	

	
	
	