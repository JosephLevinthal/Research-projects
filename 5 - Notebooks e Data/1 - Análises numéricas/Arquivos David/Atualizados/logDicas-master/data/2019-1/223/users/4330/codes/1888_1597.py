from numpy import*

custo=array(eval(input("Vetor de custo de itens: ")))
custo= custo.T
valor=80
desconto=5 # acima de 80 R$reais
i = 0
for i in range(shape(custo)[i]):
	if (custo[i]) < valor:
		custo = custo
	elif (custo[i]) >= valor:
		ct= (sum(custo)) - (desconto*(shape
		
print(ct)

