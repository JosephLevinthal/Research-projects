from numpy import*
from numpy.linalg import*

mat= array(eval(input("Digite a Matriz: ")))

for i in mat[:,j]:
	for j in mat[i,:]:
		while (mat[i] == mat[j]):
			valor= size(mat)
		print(round(valor),2)